services:
  web_app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - .env
    volumes:
      - .:/app
      - ./data:/data
      - $HOME/sep-rep-learning:/sep-rep-learning
      - $HOME/.ssh/id_ed25519:/root/.ssh/id_ed25519:ro
    ports:
      - "8889:8080"
